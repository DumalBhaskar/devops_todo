
on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: 'test'
      shell: bash
      run: |
         ls -la 
         pwd
         cd ..
         ls -la
         cd devops_todo
         ls -la
         cd "$GITHUB_WORKSPACE"
         ls -la
